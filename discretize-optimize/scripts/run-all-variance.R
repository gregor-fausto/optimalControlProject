rm(list=ls(all=TRUE)) # clear R environment

inits = c(P=10,V=1,I=0,L=0.0001)
other = c(pen=0,obj=0)
mParms = c(1,1)
mu = 2.5; sigma = .5;
source("~/Dropbox/optimalControlProject/discretize-optimize/scripts/unbranched-determinate.R")

rm(list=ls(all=TRUE)) # clear R environment

inits = c(P=10,V=1,I=0,L=0.0001)
other = c(pen=0,obj=0)
mParms = c(1,1)
mu = 2.5; sigma = 1;
source("~/Dropbox/optimalControlProject/discretize-optimize/scripts/unbranched-determinate.R")

rm(list=ls(all=TRUE)) # clear R environment

inits = c(P=10,V=1,I=0,L=0.0001)
other = c(pen=0,obj=0)
mParms = c(1,1)
mu = 2.5; sigma = 1.5;
source("~/Dropbox/optimalControlProject/discretize-optimize/scripts/unbranched-determinate.R")

rm(list=ls(all=TRUE)) # clear R environment

inits = c(P=10,V=1,I=0,L=0.0001)
other = c(pen=0,obj=0)
mParms = c(1,1)
mu = 2.5; sigma = 2;
source("~/Dropbox/optimalControlProject/discretize-optimize/scripts/unbranched-determinate.R")

rm(list=ls(all=TRUE)) # clear R environment

inits = c(P=10,V=1,I=0,L=0.0001)
other = c(pen=0,obj=0)
mParms = c(1,1)
mu = 2.5; sigma = 5;
source("~/Dropbox/optimalControlProject/discretize-optimize/scripts/unbranched-determinate.R")

rm(list=ls(all=TRUE)) # clear R environment

inits = c(P=10,V=1,I=0,L=0.0001)
other = c(pen=0,obj=0)
mParms = c(1,1)
mu = 2.5; sigma = .5;
source("~/Dropbox/optimalControlProject/discretize-optimize/scripts/branched-determinate.R")

rm(list=ls(all=TRUE)) # clear R environment

inits = c(P=10,V=1,I=0,L=0.0001)
other = c(pen=0,obj=0)
mParms = c(1,1)
mu = 2.5; sigma = 1;
source("~/Dropbox/optimalControlProject/discretize-optimize/scripts/branched-determinate.R")

rm(list=ls(all=TRUE)) # clear R environment

inits = c(P=10,V=1,I=0,L=0.0001)
other = c(pen=0,obj=0)
mParms = c(1,1)
mu = 2.5; sigma = 1.5;
source("~/Dropbox/optimalControlProject/discretize-optimize/scripts/branched-determinate.R")
        
